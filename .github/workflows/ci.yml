# .github/workflows/ci.yml
name: CI Job

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  formatting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Rust stable
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          components: "rustfmt"
          override: true
      - name: Formating check
        run: cargo fmt --check

  tests:
    name: Rust ${{ matrix.rust }} (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
#          - os: macos-latest
#            target: x86_64-apple-darwin
#            suffix: ''
#            rust: stable
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            suffix: ''
            rust: stable
#          - os: windows-latest
#            target: x86_64-pc-windows-msvc
#            suffix: .exe
#            rust: stable
    env:
      RUST_BACKTRACE: full
      RUSTV: ${{ matrix.rust }}
      # the integration tests run in their own job, they need a musl build
      WS_TEST_SKIP: "1"
    steps:
      - name: Install dependencies
        run: sudo apt-get -y install protobuf-compiler
      - uses: actions/checkout@v2
      - name: Install Rust ${{ matrix.rust }}
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust }}
          profile: minimal
          components: "clippy, rustfmt"
          override: true
      - name: Cache registry, build data
        uses: actions/cache@v2
        continue-on-error: false
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ./target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo
      - name: Lint
        run: cargo clippy
      - name: Test
        run: cargo test

  integration-tests:
    name: Integration tests (containers)
    runs-on: ubuntu-latest
    env:
      RUST_BACKTRACE: full
    steps:
      - name: Install dependencies
        run: sudo apt-get -y install protobuf-compiler musl-tools
      - uses: actions/checkout@v2
      - name: Install Rust stable
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true
          target: x86_64-unknown-linux-musl
      - name: Load kernel modules for the container network tests
        run: sudo modprobe wireguard && sudo modprobe vxlan
      - name: Cache registry, build data
        uses: actions/cache@v2
        continue-on-error: false
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ./target/
          key: ${{ runner.os }}-cargo-it-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-it
      - name: Build statically linked wirespider binary
        run: cargo build --target x86_64-unknown-linux-musl --bin wirespider
      - name: Run integration tests
        run: cargo test --test integration_tests -- --nocapture
